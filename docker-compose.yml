services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - /app/.venv
    environment:
      - DEBUG=1
    command: python mysite/manage.py runserver 0.0.0.0:8000
